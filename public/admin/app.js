const recordsBody=document.getElementById('recordsBody'),statusText=document.getElementById('statusText'),emptyText=document.getElementById('emptyText'),refreshBtn=document.getElementById('refreshBtn'),monitorToggleBtn=document.getElementById('monitorToggleBtn'),monitorDot=document.getElementById('monitorDot'),monitorState=document.getElementById('monitorState'),monitorTime=document.getElementById('monitorTime'),monitorDetail=document.getElementById('monitorDetail'),usdtFilterBtn=document.getElementById('usdtFilterBtn'),totalCount=document.getElementById('totalCount'),successCount=document.getElementById('successCount'),pendingCount=document.getElementById('pendingCount'),totalUsdt=document.getElementById('totalUsdt'),blacklistForm=document.getElementById('blacklistForm'),blacklistBody=document.getElementById('blacklistBody'),blacklistEmpty=document.getElementById('blacklistEmpty'),addressBlacklistForm=document.getElementById('addressBlacklistForm'),addressBlacklistBody=document.getElementById('addressBlacklistBody'),addressBlacklistEmpty=document.getElementById('addressBlacklistEmpty');let withUsdt=false,monitorEnabled=true,monitorPollingTimer=null,manualRefreshPending=false,recordsRequestId=0;
function escapeHtml(value){return String(value??'').replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;').replaceAll("'",'&#39;')}function formatTime(value){return value?new Intl.DateTimeFormat('zh-CN',{dateStyle:'short',timeStyle:'medium'}).format(new Date(value)):'-'}function badge(status){const type=status==='COMPLETED'||status==='success'?'success':status==='FAILED'||status==='failed'?'danger':'pending';return `<span class="badge ${type}">${escapeHtml(status||'-')}</span>`}async function readJson(res){const data=await res.json().catch(()=>({}));if(!res.ok)throw new Error(data.error||'请求失败');return data}
async function copyText(button){const text=button.dataset.copy;if(!text)return;await navigator.clipboard.writeText(text);const original=button.textContent;button.textContent='已复制';setTimeout(()=>button.textContent=original,1200)}
function renderMonitor(monitor={}){monitorEnabled=monitor.enabled!==false;monitorToggleBtn.textContent=monitorEnabled?'关闭自动监控':'开启自动监控';monitorToggleBtn.classList.toggle('is-off',!monitorEnabled);monitorDot.classList.toggle('is-off',!monitorEnabled);monitorDot.classList.toggle('is-running',Boolean(monitor.running));monitorState.textContent=monitor.running?(monitorEnabled?'正在查询 TronGrid':'自动监控已关闭，当前批次即将完成'):(monitorEnabled?'自动监控运行中':'自动监控已关闭');monitorTime.textContent=`最近查询时间：${formatTime(monitor.lastFinishedAt)}`;const result=monitor.lastResult||{};monitorDetail.textContent=monitor.lastError?monitor.lastError:`每分钟自动查询；上次检查 ${result.checked||0} 个地址，更新 ${result.updated||0} 个`}
function renderRecords(records){recordsBody.innerHTML=records.map(item=>`<tr><td class="time-cell">${formatTime(item.runpodCreatedAt)}</td><td><div class="user-cell"><div><small>名字</small><strong>${escapeHtml(item.firstName||'-')}</strong></div><div><small>用户名</small><span>${item.username?`@${escapeHtml(item.username)}`:'-'}</span></div><small>Telegram ID ${escapeHtml(item.userId)}</small></div></td><td><div class="status-stack">${badge(item.runpodStatus)}${badge(item.energyStatus)}</div></td><td><div class="address-stack"><div><span>需要能量</span><code>${escapeHtml(item.fromAddress||'-')}</code></div><div><span>目标地址</span><code>${escapeHtml(item.toAddress||'-')}</code></div></div></td><td><code class="full-address">${escapeHtml(item.resultAddress||'-')}</code></td><td><button class="copy-value" data-copy="${escapeHtml(item.privateKey)}" ${item.privateKey?'':'disabled'}>复制私钥</button></td><td><div class="balance-cell"><strong>${Number(item.runpodAddressUsdt||0).toFixed(6)}</strong><small>查询于 ${formatTime(item.usdtBalanceCheckedAt)}</small></div></td><td>${escapeHtml(item.energyCount||'-')}</td></tr>`).join('');emptyText.classList.toggle('hidden',records.length>0);document.querySelectorAll('[data-copy]').forEach(button=>button.addEventListener('click',()=>copyText(button)))}
async function loadRecords(){const requestId=++recordsRequestId,requestedWithUsdt=withUsdt;statusText.textContent='正在加载...';try{const data=await readJson(await fetch(`/api/admin/records?limit=500&withUsdt=${requestedWithUsdt}`));if(requestId!==recordsRequestId)return;renderRecords(data.records||[]);renderMonitor(data.monitor||{});const stats=data.stats||{};totalCount.textContent=stats.totalCount||0;successCount.textContent=stats.successCount||0;pendingCount.textContent=stats.pendingCount||0;totalUsdt.textContent=Number(stats.totalUsdt||0).toFixed(6);statusText.textContent=requestedWithUsdt?`当前有 USDT 的返回地址：${data.records.length} 条`:`已加载 ${data.records.length} 条`}catch(err){if(requestId===recordsRequestId)statusText.textContent=err.message}}
async function loadMonitor(){try{const data=await readJson(await fetch('/api/admin/monitor'));renderMonitor(data.monitor||{});if(!data.monitor?.running&&!manualRefreshPending){refreshBtn.disabled=false;refreshBtn.textContent='立即查询余额'}}catch(err){monitorState.textContent='监控状态读取失败';monitorDetail.textContent=err.message}}
async function refreshBalances(){manualRefreshPending=true;refreshBtn.disabled=true;refreshBtn.textContent='正在查询...';try{const data=await readJson(await fetch('/api/admin/monitor/refresh',{method:'POST'}));renderMonitor(data.monitor||{});await loadRecords()}catch(err){window.alert(err.message);await loadMonitor()}finally{manualRefreshPending=false;refreshBtn.disabled=false;refreshBtn.textContent='立即查询余额'}}async function toggleMonitor(){monitorToggleBtn.disabled=true;try{const data=await readJson(await fetch('/api/admin/monitor',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({enabled:!monitorEnabled})}));renderMonitor(data.monitor||{})}catch(err){window.alert(err.message)}finally{monitorToggleBtn.disabled=false}}
function renderBlacklist(users){blacklistBody.innerHTML=users.map(user=>`<tr><td>${escapeHtml(user.userId)}</td><td>${escapeHtml(user.firstName||'-')}</td><td>${user.username?`@${escapeHtml(user.username)}`:'-'}</td><td>${escapeHtml(user.reason)}</td><td>${user.source==='automatic'?'系统自动':'手动'}</td><td>${formatTime(user.createdAt)}</td><td><button class="danger-btn" data-remove="${escapeHtml(user.userId)}">移除</button></td></tr>`).join('');blacklistEmpty.classList.toggle('hidden',users.length>0);document.querySelectorAll('[data-remove]').forEach(button=>button.addEventListener('click',async()=>{if(!window.confirm(`确认移除用户 ${button.dataset.remove}？`))return;button.disabled=true;try{await readJson(await fetch(`/api/admin/blacklist/${encodeURIComponent(button.dataset.remove)}`,{method:'DELETE'}));await loadBlacklist()}catch(err){window.alert(err.message);button.disabled=false}}))}
function renderAddressBlacklist(addresses){addressBlacklistBody.innerHTML=addresses.map(item=>`<tr><td><code class="full-address">${escapeHtml(item.address)}</code></td><td>${escapeHtml(item.userId||'-')}</td><td>${escapeHtml(item.firstName||'-')}</td><td>${item.username?`@${escapeHtml(item.username)}`:'-'}</td><td>${escapeHtml(item.reason)}</td><td>${item.source==='automatic'?'系统自动':'手动'}</td><td>${formatTime(item.createdAt)}</td><td><button class="danger-btn" data-remove-address="${escapeHtml(item.address)}">移除</button></td></tr>`).join('');addressBlacklistEmpty.classList.toggle('hidden',addresses.length>0);document.querySelectorAll('[data-remove-address]').forEach(button=>button.addEventListener('click',async()=>{if(!window.confirm(`确认移除地址 ${button.dataset.removeAddress}？`))return;button.disabled=true;try{await readJson(await fetch(`/api/admin/blacklist/address/${encodeURIComponent(button.dataset.removeAddress)}`,{method:'DELETE'}));await loadBlacklist()}catch(err){window.alert(err.message);button.disabled=false}}))}
async function loadBlacklist(){try{const data=await readJson(await fetch('/api/admin/blacklist'));renderBlacklist(data.users||[]);renderAddressBlacklist(data.addresses||[])}catch(err){window.alert(err.message)}}
addressBlacklistForm.addEventListener('submit',async event=>{event.preventDefault();const submitButton=addressBlacklistForm.querySelector('button[type="submit"]');submitButton.disabled=true;try{await readJson(await fetch('/api/admin/blacklist/address',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({address:document.getElementById('blacklistAddress').value.trim(),reason:document.getElementById('blacklistAddressReason').value.trim()})}));addressBlacklistForm.reset();await loadBlacklist()}catch(err){window.alert(err.message)}finally{submitButton.disabled=false}});blacklistForm.addEventListener('submit',async event=>{event.preventDefault();const submitButton=blacklistForm.querySelector('button[type="submit"]');submitButton.disabled=true;try{await readJson(await fetch('/api/admin/blacklist',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({userId:document.getElementById('blacklistUserId').value.trim(),firstName:document.getElementById('blacklistFirstName').value.trim(),username:document.getElementById('blacklistUsername').value.trim(),reason:document.getElementById('blacklistReason').value.trim()})}));blacklistForm.reset();await loadBlacklist()}catch(err){window.alert(err.message)}finally{submitButton.disabled=false}});
document.querySelectorAll('[data-view]').forEach(button=>button.addEventListener('click',()=>{document.querySelectorAll('[data-view]').forEach(item=>item.classList.remove('active'));document.querySelectorAll('.admin-view').forEach(view=>view.classList.remove('active'));button.classList.add('active');document.getElementById(button.dataset.view).classList.add('active');if(button.dataset.view==='blacklistView')loadBlacklist()}));usdtFilterBtn.addEventListener('click',()=>{withUsdt=!withUsdt;usdtFilterBtn.classList.toggle('active',withUsdt);usdtFilterBtn.textContent=withUsdt?'显示全部记录':'只看当前有 USDT';loadRecords()});refreshBtn.addEventListener('click',refreshBalances);monitorToggleBtn.addEventListener('click',toggleMonitor);monitorPollingTimer=setInterval(loadMonitor,5000);window.addEventListener('beforeunload',()=>clearInterval(monitorPollingTimer));loadRecords();loadBlacklist();